#include "holberton.h"
/**
 * is_prime_number - finds prime numbers
 * @n: parameter integer
 * Return: 1 if true, otherwise 0
 */

int is_prime_number(int n)
{
    if (n % 1 = 0 && n % n = 0)
        {
            
        }
}